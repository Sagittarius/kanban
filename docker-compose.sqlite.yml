services:
  kanban:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    environment:
      - KANBAN_SQLITE_PATH=/data/kanban.sqlite
    volumes:
      - kanban-data:/data
    restart: unless-stopped

volumes:
  kanban-data:
