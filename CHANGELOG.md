# Changelog

## 1.4.1 - 2026-06-29
- 后台管理看板页补充编辑能力，支持修改看板名称、说明、关联团队，并允许调整看板拥有者。
- 放宽项目经理与开发经理的用户管理和看板授权可见范围，改为面向系统全量用户，同时保留超管角色保护。
- 修复后台管理单选搜索选择器在不同浏览器下的收起与回显一致性问题。
- 优化后台管理看板详情操作区文案与按钮布局，统一为更紧凑的 `编辑`、`删除` 操作。
- 修复后台相关类型定义细节，恢复 1.4.1 发布前的全量 TypeScript 校验。

## 1.4.0 - 2026-06-28
- 引入统一结构化 JSON 运行日志，API 请求自动记录 requestId、方法、路径、状态码、耗时、IP 和 UA。
- 支持通过 `KANBAN_LOG_LEVEL`、`KANBAN_LOG_CONSOLE`、`KANBAN_LOG_FILE`、`KANBAN_LOG_DIR` 配置控制台和文件日志。
- 新增 `audit_logs` 审计表，记录登录、退出、用户管理、看板、团队、系统参数、项目、任务和拆解任务等关键操作。
- 后台管理新增 `审计` 页签，支持查看和搜索最近审计记录。
- SQLite、PostgreSQL 和 SQLite 到 PostgreSQL 迁移链路补齐审计日志表。
- Docker 运行镜像改为只安装生产依赖，并默认将容器日志文件写入 `/data/logs/kanban.log`。
- `.dockerignore` 排除 `_deliverables`、本地构建缓存和系统临时文件，避免内网部署包和本地文件进入构建上下文。
- 拉齐项目经理与开发经理权限，团队成员保留项目详情查看能力。
- 项目新增结构化负责人字段，创建和编辑项目时负责人从关联团队成员中选择。
- 新建和编辑任务时要求先选择归属项目，再按项目关联团队选择负责人和测试员。
- 项目负载大屏按角色、团队和公开访问参数控制数据范围，修复开发经理负载数据可见性。
- 优化项目负载人员状态、项目详情任务列表、个人名片、技术栈折叠和任务风险标识展示。
- 看板左侧栏重构项目导航、任务筛选和新任务区域层次，统一必填校验和中文提示。
- 新增统一确认对话框，替换原生浏览器确认和提醒。
- 统一看板单选搜索选择器浮层、清空搜索、重置选择和下拉层级，修复抽屉内样式污染与遮挡问题。
- 修复升级环境中后台管理看板接口异常时前端白屏的问题，并增强旧库看板成员与团队关联表缺失时的兼容降级。
- 同步 1.3.2 用户资料字段 repair migration，避免旧库缺少 `users.display_name` 等列影响 1.4.0 后续迁移。
- 禁用大屏与后台页面导航入口的 RSC 预取，避免生产环境出现 `vinext` prefetch 噪声报错。
- 项目负载大屏为人员状态、分布、负载排行和任务池配置差异化语义图标。
- 文件日志支持按大小滚动、按数量和保留天数清理，并支持 `KANBAN_LOG_FILE_ENABLED=false` 仅保留控制台日志。

## 1.3.2 - 2026-06-28
- 修复跨列拖拽插入非末尾位置时可能触发 `removeChild` 运行时异常的问题。
- 取消看板页新建任务和卡片拖拽的团队成员权限尝试，保留项目 CRUD 权限控制。
- 负载面板无权访问页按钮文案调整为 `进入看板`。

## 1.3.1 - 2026-06-28
- 增加 `开发经理` 角色，并补充用户职位、技术栈画像字段。
- 用户信息增加手机号，后台用户管理与个人名片同步展示。
- 任务增加工作量人日字段，未填写时负载计算按 1 人日处理。
- 个人设置、后台管理、负载面板统一支持职位与技术栈展示。
- 统一多选选择器交互，支持搜索、胶囊回显和图标化清空。
- 看板顶部统计增加 `超期` 维度，列表视图阶段筛选改为更直观的多选器样式。
- 优化看板任务统计、工作量展示、多选搜索选择器与单选清空交互。
- 看板进入时默认卡片视图，避免恢复列表视图造成初始闪烁。
- 后台管理增加看板删除入口，并优化用户、团队、看板列表的展示层次。
- 非登录模式新部署会初始化系统参数 `board_title` 指定名称的默认看板，升级部署会展示数据库里最早创建的看板。
- 登录成功后默认进入项目负载页
- `工作负载面板` 更名为 `项目负载大屏`，并在看板页新增项目负载悬浮入口。
- 项目负载大屏升级为多团队、多项目筛选，新增临期、超期、阻塞总览。
- 项目负载大屏增加项目详情、成员详情、任务详情联动弹层。
- 项目负载大屏将 `测试中` 阶段任务计入测试员负载，其余阶段计入负责人负载。
- 项目负载大屏按人日加权计算平均进度与负载排行。
- 优化项目负载人员状态、个人名片、任务详情、标签、进度与风险提示样式。
- 调整项目负载算法，按职位区分开发与测试负载，并支持测试工作量系统参数。
- 修复非登录模式默认看板初始化、系统参数保存、主题切换闪烁与任务完成状态联动。
- SQLite 与 PostgreSQL 升级链路继续维护，新增用户画像扩展迁移脚本。

## 1.3.0 - 2026-06-26
- 增加团队管理、看板管理与工作负载面板基础能力。

## 1.2.0
- 增加鉴权、用户管理等功能。
- 支持 SQLite / PostgreSQL 双驱动及数据库升级脚本。

## 1.1.0

- 看板基础版本，包含项目、任务、子步骤、活动记录、主题与 Docker 部署能力。
